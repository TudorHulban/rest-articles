version: '3.9'

services:
  db:
    profiles: ["run", "unittest"]
    restart: always
    image: postgres:15-alpine
    container_name: cn_rest_db
    environment:
      - POSTGRES_DB=rest
      - POSTGRES_USER=postgres
      - POSTGRES_HOST_AUTH_METHOD=trust 
    ports:
      - 5432:5432

  rest:
    profiles: ["run"]
    build: .
    container_name: cn_rest
    environment:
      - POSTGRES_DATABASE=rest
    command: /app/main
    ports:
      - 3000:3000
    depends_on:
      - db